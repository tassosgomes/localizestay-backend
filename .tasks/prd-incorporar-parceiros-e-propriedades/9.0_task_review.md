# Revisão da Tarefa 9.0 — Encaminhamento, devolução, encerramento e outbox

## Resultado da validação automatizada

| Comando | Resultado |
|---|---|
| `dotnet test tests/LocalizeStay.UnitTests/LocalizeStay.UnitTests.csproj --filter "FullyQualifiedName~SubmissionCommandHandlerTests" --no-restore` | Aprovado — 4 testes passaram, 0 avisos. |
| `dotnet test tests/LocalizeStay.IntegrationTests/LocalizeStay.IntegrationTests.csproj --filter "FullyQualifiedName~PropertyOnboardingWorkflowTests" --no-restore` | Aprovado — 1 teste passou, 0 avisos. |
| `dotnet test tests/LocalizeStay.IntegrationTests/LocalizeStay.IntegrationTests.csproj --filter "FullyQualifiedName~OutboxAndAuditTests" --no-restore` | Aprovado — 2 testes passaram, 0 avisos. |
| `dotnet build LocalizeStay.sln --no-restore` | Aprovado — 24 projetos, 0 erros, 0 avisos. |
| `dotnet format LocalizeStay.sln --verify-no-changes --no-restore` | Aprovado — nenhum arquivo exigiu formatação. |
| `git diff --check` | Aprovado — sem erros de whitespace. |

## Revisão técnica

O submit grava lifecycle, auditoria, idempotência e outbox no mesmo `SaveChangesAsync`; a integração com PostgreSQL cobre rollback quando a inserção na outbox falha. O replay por chave devolve o resultado persistido, enquanto payload incompatível retorna `STATE_CONFLICT`. A devolução preserva o ciclo anterior e cria pendências; o encerramento exige motivo e permite novo ciclo. As três rotas usam as policies previstas, e o source/meter de lifecycle está registrado no pipeline OpenTelemetry sem incluir PII nas métricas.

## Problemas encontrados

Nenhum.

## Recomendação final

**APROVADA**
